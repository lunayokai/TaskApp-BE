const express = require('express');
const router = express.Router();
//const {registerUser,confirmRegister, loginUser,recoverPassword, getUser, editUser, getLogout, deleteUser, newPassword, getUsers,} = require('../../controllers/api/UsersApiController')
//const loginValidations = require("../../validations/users/loginValidations");
//const editValidations = require("../../validations/users/editValidations");
//const recoverPasswordValidations = require("../../validations/users/recoverPasswordValidations");
//const newPasswordValidation = require("../../validations/users/newPasswordValidation");
//const authMiddleware = require('../../middlewares/authMiddleware');

//rutas de api/users;

//router.get('/', getUsers);
//router.post('/register', registerValidations, registerUser);
//router.get('/register-confirmation/:token',confirmRegister);
//router.post('/login', loginValidations, loginUser);
//router.get('/logout', getLogout);
//router.get('/:id',authMiddleware, getUser);
//router.post('/authMe', authMiddleware, getUserData)
//router.post('/password-recovery',recoverPasswordValidations, recoverPassword);
//router.post('/new-password',newPasswordValidation, newPassword);
//router.put('/:id',authMiddleware,editValidations, editUser);
//router.delete('/:id',AdminAuthMiddleware, deleteUser);

module.exports = router;